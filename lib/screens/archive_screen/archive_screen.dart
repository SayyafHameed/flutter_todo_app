import 'package:flutter/material.dart';
class AppArchiveScreen extends StatelessWidget {
  const AppArchiveScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Archive', style: TextStyle(fontSize: 20.0,),),

    );
  }
}