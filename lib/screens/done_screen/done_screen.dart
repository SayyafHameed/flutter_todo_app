import 'package:flutter/material.dart';
class AppDoneScreen extends StatelessWidget {
  const AppDoneScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Done', style: TextStyle(fontSize: 20.0,),),

    );
  }
}
